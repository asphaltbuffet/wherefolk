// Package version provides build version information for wherefolk.
// Version, GitCommit, and BuildDate are injected at build time via ldflags;
// see the build task in mise.toml and the ldflags in .goreleaser.yml.
package version
